"""Общая настройка тестов.

Сервисный слой читает Config.* напрямую из базового класса (а не из app.config),
поэтому переопределение в TestingConfig туда не доходит. Отключаем тяжёлую модель
эмбеддингов (уровень 2 дообучения) здесь — до импорта приложения и тестов, —
чтобы тесты не грузили sentence-transformers (~120 МБ) и шли быстро.
"""
from config import Config

Config.SEMANTIC_MATCH_ENABLED = False
