# === Web / API ===
Flask==3.1.2
flask-cors==6.0.1
Flask-JWT-Extended==4.7.1
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.44
alembic==1.17.1
python-dotenv==1.0.0
gunicorn==23.0.0

# === Database ===
psycopg2-binary==2.9.10        # PostgreSQL driver

# === Processing queue ===
celery==5.4.0
redis==5.2.1

# === Data extraction (TZ 4.2) ===
pdfplumber==0.11.4             # text PDF + tables
PyMuPDF==1.25.1                # fast PDF parsing (fitz)
python-docx==1.1.2             # DOCX + tracked changes
openpyxl==3.1.5                # XLSX
xlrd==2.0.1                    # legacy XLS (бинарный .xls, openpyxl его не читает)
pandas==2.2.3                  # tabular post-processing

# === OCR (scans) ===
# EasyOCR — готовая предобученная нейросеть (без системного бинаря Tesseract и
# без poppler: PDF рендерим через PyMuPDF). Тянет torch/opencv; на первом запуске
# скачивает веса моделей в кэш (~80 МБ на язык), дальше работает офлайн.
easyocr==1.7.2
Pillow==11.0.0

# === Normalization / fuzzy matching (TZ 4.3) ===
rapidfuzz==3.10.1
# sentence-transformers==3.3.1  # optional: semantic matching via embeddings

# === External HTTP (optional cloud OCR / LLM) ===
requests==2.32.5

# === Tests ===
pytest==9.0.2
