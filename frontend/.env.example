# URL бэкенда MedArchive.
# В dev оставьте пустым/закомментированным — клиент ходит на относительный /api,
# а CRA-прокси (см. "proxy" в package.json) перенаправляет на http://localhost:5252.
# Для прод-сборки укажите абсолютный адрес API:
# REACT_APP_API_URL=https://medarchive.example.kz/api
